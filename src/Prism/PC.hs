module Prism.PC (
        ------------------------------------------------------
        PC (..), PeripheralsPC
        , createPC, pcPorts
        , getPC, putPC
        ------------------------------------------------------
    ) where

import Prism.PC.PC
import Prism.PC.Pic

-------------------------------------------------------------------------------
